#include<stdio.h>
#include<conio.h>
main(){
	int i,n,k,h;
	for(i=1;i<=5;i++)
	{
		for(n=1;n<=i;n++)
		{
			printf("%d ",n);
		}
		printf("\n");
	}
	for(k=4;k>=1;k--)
	{
		for(h=1;h<=k;h++)
		{
			printf("%d ",h);
		}
		printf("\n");
	}
}
